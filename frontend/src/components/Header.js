import React from 'react';
import { HeaderContainer, Title } from '../styles/AppStyles';

const Header = () => (
  <HeaderContainer>
    <Title>Analyze Your Data</Title>
  </HeaderContainer>
);

export default Header;
